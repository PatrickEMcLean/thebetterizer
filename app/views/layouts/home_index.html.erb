<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Betterizer" %></title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/3.6.1/html5shiv.js" type="text/javascript"></script>
    <![endif]-->

    <%= stylesheet_link_tag "application", :media => "all" %>

  </head>
  <body>
    <div class="wrapper">
    <div class="navbar navbar-fixed-top">
      <div class="navbar-inner">
        <div class="container">
          <div class="pull-right" style="padding-top:8px">
            <% if current_user %>
              Welcome, <%= current_user.name %> |
              <%= link_to "sign out", signout_path %>
            <% else %>
              <%= link_to "sign in with Google", "/auth/google_oauth2" %>
            <% end %>
          </div>
          <a class="btn btn-navbar" data-target=".nav-collapse" data-toggle="collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </a>
          <a class="brand" href="#">The Betterizer</a>
          <div class="container nav-collapse">
            <ul class="nav">
            </ul>
          </div><!--/.nav-collapse -->
        </div>
      </div>
    </div>

    <div class="hero-unit">
      <div class="container">
        <h1 style="color:white">The Betterizer</h1>
        <h3 style="color:white; padding-left: 10px;">be better at the things you do while you get things done</h3>
      </div>
    </div>

    <div class="container clear-top">
        <div class="row-fluid">
          <div class="span12">
            <%= bootstrap_flash %>
            <%= yield %>
          </div>
        </div><!--/row-->
      </div>
    </div>
  </div>
      <footer class="navbar-inner">
        <div class="container">
          <div class="span12">
            <p>&copy; The Betterizer 2013</p>
          </div>
        </div>
      </footer>

    </div> <!-- /container -->

    <!-- Javascripts
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <%= javascript_include_tag "application" %>

  </body>
</html>
